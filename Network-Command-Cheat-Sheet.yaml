---
commands:
  enable:
    desc: "Used to enter privilaged mode"
    make:
      cisco: enable
      hpe: system-view
  config:
    desc: "Used to enter global config mode"
    make:
      cisco: conf t
      hpe: system-view